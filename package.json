{
  "name": "spare-parts",
  "private": true,
  "scripts": {
    "install:all": "cd apps/backend && npm install && cd ../frontend && npm install",
    "build": "cd apps/frontend && npm install && npm run build && cd ../backend && npm install && npm run build && npx prisma generate",
    "start": "cd apps/backend && npx prisma migrate deploy && node dist/main",
    "dev:backend": "cd apps/backend && npm run start:dev",
    "dev:frontend": "cd apps/frontend && npm run dev",
    "db:migrate": "cd apps/backend && npx prisma migrate dev",
    "db:generate": "cd apps/backend && npx prisma generate",
    "db:seed": "cd apps/backend && npm run db:seed"
  }
}
