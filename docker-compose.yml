version: '3.8'

services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: spare
      POSTGRES_PASSWORD: spare
      POSTGRES_DB: spare_parts
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
